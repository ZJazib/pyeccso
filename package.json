{
  "name": "pyecso-php",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nix run nixpkgs#php -- -S 0.0.0.0:8080 -t public_html public_html/index.php",
    "build": "mkdir -p dist && cp -r public_html/. dist/",
    "build:dev": "mkdir -p dist && cp -r public_html/. dist/",
    "start": "nix run nixpkgs#php -- -S 0.0.0.0:8080 -t public_html public_html/index.php"
  }
}
